# 本文件完全可选。直接执行 `docker compose up -d` 即可使用全部默认值。
# 只有需要调整以下少数项目时，才复制为 `.env`：
#
#   cp .env.example .env

# 固定正式版本可避免更新时自动跨版本；默认使用 latest。
APP_VERSION=latest

# Web 访问端口。
APP_PORT=5251

# 容器时区。
TZ=Asia/Shanghai

# 搬运时可能暂存完整视频。系统盘空间不足时改成大容量磁盘目录。
RCLONE_TEMP_HOST_PATH=./rclone/temp
